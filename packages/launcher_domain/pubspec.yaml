name: launcher_domain
description: UI-independent TopiaForge launcher domain models and planning logic.
version: 0.1.1
publish_to: none

environment:
  sdk: ^3.11.1

dependencies:
  unorm_dart: ^0.3.2

dev_dependencies:
  json_schema: ^5.2.2
  lints: ^6.0.0
  test: ^1.25.6
