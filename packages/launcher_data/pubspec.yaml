name: launcher_data
description: Local filesystem repositories for the TopiaForge standalone launcher.
version: 0.1.1
publish_to: none

environment:
  sdk: ^3.11.1

dependencies:
  archive: ^4.0.7
  crypto: ^3.0.6
  launcher_domain:
    path: ../launcher_domain
  path: ^1.9.1
  unorm_dart: ^0.3.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
