# Example Sample

Put a sample scene or assets here. The `Samples~` folder is not imported by default; users import it from the
Package Manager window. Reference it in `package.json` under `samples`.
